"""Sondas que se ejecutan A MANO, con Resolve abierto.

Esto NO es parte de la app. Es lo unico del repo que importa
`DaVinciResolveScript`, y lo hace dentro de una funcion, nunca al cargarse.

`api_probe.py` esta escrito para poder copiarse suelto a cualquier sitio y
ejecutarse con el `python3` del sistema: solo usa la biblioteca estandar y no
importa nada de `core`.
"""
