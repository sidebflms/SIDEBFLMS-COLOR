"""Calibración de `core.reverse.confianza_destino` (día 5, tarea 2).

Paquete hermano de `tests/calibracion/` (día 4): mismo montaje, mismo material
sintético, mismo criterio de bootstrap por escena. Lo único nuevo es QUÉ se
mide: las cuatro señales de `FeaturesDestino` en vez de las subnotas de
`core.matching.confianza`. Ver `tests/calibracion_destino/NOTAS.md` (si existe)
y `CALIBRACION-CONFIANZA-DESTINO.md` en la raíz para el veredicto.
"""
